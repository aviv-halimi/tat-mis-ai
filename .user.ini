; PHP upload limits for menu PDFs and other file uploads.
; Plesk / Apache (mod_php) will read this from the document root.
; Ensure post_max_size >= upload_max_filesize.
upload_max_filesize = 64M
post_max_size = 64M
